Inside of src/network.ts some configuration is required.
The contractAcct and the tokenAcct must be set.
If you use the jungle4 testnet (recommended) 
	and TTT as the token symbol then you won't need to change the other values.